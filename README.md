# cdk-apigw-sqs-lambda

Example CDK app for API Gateway with SQS and Lambda.

## How to deploy

```bash
git clone https://github.com/mats16/cdk-apigw-sqs-lambda.git
cd cdk-apigw-sqs-lambda

yarn install

npx projen deploy
```
